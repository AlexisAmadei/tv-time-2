# Party Memlog — Core Team

## 2026-07-02 — Roundtable: TV Time 2 epic breakdown review (before story creation)

**Room:** John (PM), Winston (Architect), Sally (UX), Amelia (Dev), Murat (Test Architect). Session mode. Reviewing the 7-epic breakdown in `_bmad-output/planning-artifacts/epics.md`.

**Decisions Alex made (all folded into epics.md):**
1. **Epic 1 reshaped** from broad "Foundation & Identity" → thin **vertical walking skeleton "First Watch"**: auth + `catalog-search` (chosen over Dana's hardcode-a-tmdb_id) + log-a-watch-via-outbox + private-by-default RLS wall + tokens-defined/dark-wired. Exit test: another user can't read your watch, and a wifi-off log persists.
2. **Privacy re-sequenced** — visibility schema + private-by-default RLS moved OUT of Epic 5 INTO Epic 1 (the round-1 "genuine correctness bug"). Winston's hill to die on: privacy is the default state of every row, not a social feature. Epic 5 keeps only the share/feed/follow exception branch.
3. **Go-live gates left Epic 7** → became a cross-cutting Launch Checklist (backup ARCH-15, EU hosting ARCH-16). Epic 7 shrank to export + delete. (Dana vs Murat; Winston conceded gates ≠ features.)
4. **NFR1 (15s loop) is Epic 3's exit gate**, not Epic 1's (Murat).

**Standing asks / open loops:**
- Mood-chip set (FR18 8-chip vs DESIGN.md 5-chip) must be LOCKED before the Epic 3 rating component. Still unresolved — Sally will not build it twice.
- John's ask: import was cut from v1 (no export obtained); PRD still lists FR38–41/Goal #3 → recommend a correct-course PRD reconciliation.

**Table dynamics / running bits:** Dana (pragmatist) as the ship-it counterweight — "eleven users who are your friends," "One function. I'm watching you." Amelia repeatedly half-siding with Winston on invariants against her own thin-slice instinct. Sally fighting to keep the cozy first-impression from being amputated ("skeletons have a way of becoming production"). Grumbal (from the code-review crew) name-dropped as the hypothetical second user who can't read Amelia's row.

## 2026-07-02 (session 2) — Story-level review of all 32→33 stories

Same room. Boundary (edge-case hunter) crashed in from the code-review crew as an uninvited walk-on and stayed — running bit: steals chairs, "One test. Named."

**Findings Alex accepted + I folded in (option a, then wrapped):**
1. **Missing story → added Story 4.4** (Epic 4): edit display name + avatar upload to Supabase Storage. FR2's avatar had no implementing story AND Storage was provisioned-but-unused. Amelia found it; Sally: "the Profile has a heartbeat now."
2. **Story 3.7 pointer bug → fixed.** Recompute-after-delete can't use an *advance*-only RPC (won't move backward). Pointer RPC is now **derive/recompute-from-full-watch-set**, idempotent, serves both advance + recompute. Architecture note added recommending AD-10 rename `advance_next_episode_pointer` → `recompute_next_episode_pointer`. (Winston: "the invariant fixed, not just the story.")
3. **Story 3.5 hardened.** Added a **named, must-automate** regression test for the AD-4 hazard: log network-off → tap rating before sync → restore → rating+watch arrive as ONE row, never a lost PATCH. Dana held the line: add the test, don't split the story.

**Still open (Alex's call, not the room's):** Story 4.2 profile-aggregation rule (PRD OQ#10) — how "favorites"/stats derive against the multi-watch timestamped model. Genuinely undecided; story can't be built/tested until decided. Flagged `[decision needed]` in the doc.

**Story count now 33.** Next: Step 4 final validation.
