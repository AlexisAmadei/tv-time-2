# code review of stories 3.7+3.8 completed 2026-07-11: reviewed jointly (both landed uncommitted in the same working tree, no git boundary between them); 3-layer adversarial review (Blind Hunter, Edge Case Hunter, Acceptance Auditor) against git diff HEAD (22 files, +2175/-205); 4 decision-needed resolved (3.8 actually shipped as a whole new Recommendations screen/tab replacing the deleted Diary tab rather than the spec'd Home shelf — kept the screen, re-scoped the story, fixed its AC2 error/retry violation as a patch; Diary tab removal confirmed permanent, flagged as an Epic 4/4.1 follow-up; Up Next hidden on Movies tab kept as intentional; TitleCard particle/pulse animation kept as a wanted feature per 3.2/3.3 precedent; CLAUDE.md Kan.bn deletion + eas.json/app.json build-infra changes both confirmed intentional out-of-band work), 8 patches applied (watchEdit.ts affected-row checks on edit/delete, consistent session posture, date bound, dead try/catch removed, unbounded getWatchesForTitle capped; TitleDetailScreen loadWatches requestSeq guard; EditWatchSheet iOS date picker premature-close fix; RecommendationsScreen AC2 error/retry UI replaced with silent-omit), tsc/expo export clean → done
# story 3.8 implemented 2026-07-11: new app/data/recommendations.ts (12 curated ids, getRecommendations()); HomeScreen.tsx gains a third fully-independent Recommendations shelf that omits itself on any failure/empty (AC2 non-blocking — never error/retry), a best-effort watchlisted+logged de-dup filter (AC1), and the ❤️ heart reusing 2.3's writeWatchlist + TitleCard props with AddScreen's optimistic-toggle/rollback (AC3). Shelf gains onToggleWatchlist?/watchlistKeys? pass-through props (all other call sites omit → no heart on Up Next/Watchlist). wholePageEmpty (3.1) untouched — recs excluded so a new user still sees the empty-Home takeover. No new smoke-check block (no new server contract — ❤️ reuses 2.3's watchlist_items write). tsc/expo export android/smoke-check all clean; throwaway script confirmed all 12 curated ids resolve via proxied catalog-title. On-device UI pass (both tabs, heart a11y, slow-catalog non-blocking proof) outstanding — no emulator, consistent with every prior Epic 3 story. status → review
# story 3.8 started 2026-07-11: status → in-progress
# story 3.8 drafted 2026-07-11: optional Recommendations shelf on Home — deliberately the minimal FR42 interpretation. Source is a client-curated static id list (new app/data/recommendations.ts), enriched through the existing fetchTitleDetail proxy (AD-6 honored — ids only, no direct TMDB); NO new Edge Function, migration, dependency, shared-types change, or smoke-check block (first Epic 3 story with none — nothing new server-side). New third independent shelf on HomeScreen following 2.4/3.1's per-shelf phase/hasLoadedRef/requestSeq pattern; unlike Up Next/Watchlist it OMITS itself on any failure/empty (never error/retry, never blocks — AC2 is load-bearing). ❤️ reuses 2.3's writeWatchlist/getWatchlistKeys + TitleCard's existing heart props, mirroring AddScreen's optimistic-toggle/rollback (AC3). Best-effort de-dup filter vs watchlisted+logged keys. wholePageEmpty reconciliation left untouched (recs excluded — new user still sees empty-home takeover). status → ready-for-dev
# story 3.7 implemented 2026-07-11: direct-PostgREST editWatch/removeWatch/getWatchesForTitle in new app/data/watchEdit.ts (AC1 names PostgREST directly, not the outbox), new "Your watches" section + EditWatchSheet.tsx on TitleDetailScreen.tsx (title detail is the only viable entry point — Diary is still Epic 4's placeholder), reusing StarRating/MoodChipRow (3.5) and mirroring RatingPrompt/BulkLogSheet's sheet chrome. One new dependency (@react-native-community/datetimepicker@9.1.0, via expo install). removeWatch is the first call site to exercise the pointer RPC's backward-derive path (AC2); new smoke-check block 11 covers it plus cross-user edit/delete RLS — both new checks pass, idempotent across reruns. tsc/expo export/smoke-check clean; a throwaway HTTP-level verification script (not committed) exercised the full watchEdit.ts contract directly against the local stack. On-device UI pass outstanding (no emulator, consistent with every prior Epic 3 story). status → review
# story 3.7 started 2026-07-11: status → in-progress
# code review of stories 3.5+3.6 completed 2026-07-11: reviewed jointly (both landed uncommitted in the same working tree — 3.6 built directly on 3.5's outbox contract with no git boundary between them); 3-layer adversarial review (Blind Hunter, Edge Case Hunter, Acceptance Auditor) against git diff HEAD; 1 decision-needed resolved (an unauthorized Series/Movies tabs feature bundled into the diff — kept, its Up-Next/movie-visibility bug and tab-tap-before-layout bug fixed as patches), 6 patches applied (movies missing from Up Next, tab-tap-before-layout silent no-op, crash-unsafe moods migration backfill, missing triggerSync follow-up after a synced-row PATCH, note-length clamp defense-in-depth, note-debounce unmount cleanup), 3 deferred, 4 dismissed as noise; tsc clean → done
# story 3.7 drafted 2026-07-11: direct-PostgREST editWatch/removeWatch/getWatchesForTitle in new app/data/watchEdit.ts (AC1 names PostgREST directly, not the AD-4 outbox — this is an edit/remove of an already-logged watch, not the original commit) + a new "Your watches" section and EditWatchSheet.tsx on TitleDetailScreen.tsx, reusing StarRating/MoodChipRow (3.5) and mirroring RatingPrompt/BulkLogSheet's sheet chrome. Title detail is the only viable entry point — DiaryScreen.tsx is still the 1.3 placeholder (Epic 4/4.1's job; that story's own epics.md text already plans to reuse this story's data layer). removeWatch is the first call site to exercise the pointer RPC's backward-derive path (AC2 — un-logging an episode behind the pointer); new smoke-check block covers it plus cross-user edit/delete RLS. One new dependency (@react-native-community/datetimepicker) for AC1's date edit. No migration needed (0003's RLS policies already cover it). status → ready-for-dev
# story 3.6 drafted 2026-07-10: widens Story 3.5's setWatchReaction/reaction_rev outbox contract to carry note alongside rating/moods — pending_watches.note and watches.note already existed (1.5's speculative schema, comments naming this story), watchSync's upsert already sends note:row.note (dead since 1.5), insertPendingWatch already hardcodes note:null — so Task 1 is mostly verification, not new plumbing. New: debounced (not per-keystroke) note TextInput in RatingPrompt.tsx with a 500-char maxLength+counter (client-side cap only, per 0008's mood-cardinality precedent — no DB CHECK), flush-on-dismiss so a note typed just before Skip isn't lost, and a KeyboardAvoidingView (first TextInput inside a Modal sheet in this codebase). No migration, no shared-types change. Scope wall: no Diary note-editing surface (Epic 4, DiaryScreen.tsx doesn't exist yet) and no note field in BulkLogSheet (season-level rating/mood only, no note per 3.4's AC3). status → ready-for-dev
# story 3.5 implemented 2026-07-10: post-watch rating prompt (new RatingPrompt/StarRating/MoodChipRow in app/components/) fired from the two single-watch commit sites (HomeScreen.handleMarkWatched, AddScreen.handleLog), opened the instant the local commit resolves — before triggerSync/refetches — so no network sits in front of AC1. Outbox reaction plumbing: logWatch/logWatchBatch now return the client-generated id; new setWatchReaction() writes local-first then branches on synced_at (pending→drain carries commit+reaction as one row, AC3's hazard unrepresentable; synced→bounded PATCH). Local pending_watches gains moods (JSON array, replacing the now-dead singular mood), reaction_rev, and synced_rev — the rev pair guards BOTH AC3's rate-before-sync AND the unnamed rate-during-sync lost-update race (watchSync snapshots reaction_rev before the upsert and writes synced_rev=snapshot, never a re-read). watchSync's selection widened to reaction-dirty rows; moods decoded at the sync boundary; pointer recompute gated on wasUnsynced so a reaction never fires the RPC. No migration (0003 rating CHECK + 0008 locked-set CHECK already satisfy AC4). AC3's "must be automated" honored via 5 durable server-side assertions in smoke-check.mjs (block 10). tsc/expo export/smoke-check clean; on-device UI pass outstanding (no emulator). status → review
# story 3.5 started 2026-07-10: status → in-progress
# story 3.5 drafted 2026-07-10: post-watch rating prompt (new RatingPrompt.tsx + StarRating.tsx/MoodChipRow.tsx — the shared extraction 3.4's scope wall explicitly deferred here) fired from the two single-watch commit sites only (HomeScreen.handleMarkWatched, AddScreen.handleLog; BulkLogSheet keeps its own season-level control and gets no prompt). Outbox work is the real weight: logWatch/logWatchBatch must return the client-generated watch id (the prompt can't address its row otherwise); local pending_watches gains a JSON `moods` column (0–2 chips vs 3.4's singular `mood`, now dead) plus a `reaction_rev`/`synced_rev` pair that makes AC3's rate-before-sync hazard AND the unnamed rate-during-sync lost-update race unrepresentable; watchSync's drain predicate widens to reaction-only changes and gates the pointer RPC on a genuinely-new row. No migration needed (0003's rating CHECK + 0008's `<@` locked-set CHECK already satisfy AC4; mood cardinality deliberately left unconstrained per 0008's own deferral). AC3's "must be automated" honored via durable checks in scripts/smoke-check.mjs (1.6's precedent) — the client-side branch stays untestable until a test framework lands, flagged as an open question. status → ready-for-dev
# story 3.4 implemented 2026-07-10: fixed two latent 1.5/3.2 outbox/sync gaps this story is first to exercise (LogWatchInput rating/mood passthrough; watchSync's per-row-inline pointer recompute moved to once-per-pass-per-show, after the row loop, so a bulk commit's pointer reflects the WHOLE batch not just episode 1) + mood array-wrap at the sync boundary (0003's server text[] vs local text mismatch) + 0008 migration locking watches.mood to the FR18 8-chip CHECK + "Mark whole season watched" on SeasonRow + new BulkLogSheet.tsx (RN Modal, pre-checked episode list, minimal 5-star rating, single-select mood chips) + TitleDetailScreen wiring with verbatim AC4 confirmation copy; tsc/expo export/smoke-check clean; migrations re-applied; Task 1c's fix verified directly against local Supabase stack with a scenario shaped to expose the pre-fix bug (deselect the LAST episode of a 5-ep bulk commit) — pointer correctly lands on episode 5 (would have frozen at episode 2 under old code); on-device manual UI pass outstanding (no emulator) → review
# story 3.4 started 2026-07-10: status → in-progress
# generated: 2026-07-02
# last_updated: 2026-07-10
# story 3.4 drafted 2026-07-10: bulk-log sheet (new file, TitleDetailScreen's SeasonRow gains "Mark whole season watched") logs N episodes via existing logWatch()/watchSync outbox unchanged in shape; fixes two latent gaps this story is first to exercise — LogWatchInput lacked rating/mood passthrough (always null since 1.5), and watchSync's per-row-inline pointer-recompute call would fire on partial data when several episodes of the same show sync in one pass (correct for 3.2's one-episode-at-a-time case, wrong for this story's N-episode bulk commit) — moved to once-per-pass-per-show after the row loop; adds 0008 migration locking watches.mood to the FR18 8-chip CHECK per ARCHITECTURE-SPINE's Consistency Conventions; status → ready-for-dev
# project: popcorn-time
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones

# code review of stories 3.2+3.3 completed 2026-07-09: reviewed jointly since both stories' code landed in the same commit (e6163dd); 3-layer adversarial review (Blind Hunter, Edge Case Hunter, Acceptance Auditor) against diff 7228570..HEAD; 1 decision-needed resolved (out-of-scope "Watched" accordion shelf on Watchlist, not authorized by either story's scope wall — kept as a wanted feature, rough edges cleaned up), 13 patches applied (stale TitleCard comment, watchedPendingKeys per-key lifecycle race/stuck-pill fix, synchronous double-tap guard, triggerSync error logging, recompute-failure log wording, watchedPending visual feedback, UpNextItem cast consistency, watchSync dedupe-key/timeout-constant cosmetics, Watched-shelf emptyCopy/collapsedCopy/rewatch-handler/cross-shelf-refresh), 5 dismissed; tsc + android export clean → done
# code review of story 3.1 completed 2026-07-08: 3-layer adversarial review (Blind Hunter, Edge Case Hunter, Acceptance Auditor); all 4 ACs conformant, no scope-wall violations; 1 decision-needed patched (RPC p_media_type disambiguation for tmdb_id collisions across movie/tv, also updated ARCHITECTURE-SPINE.md AD-10), 1 patch applied (deduped COPY_TRACK_FAILED/COPY_WATCHLIST_FAILED into COPY_SAVE_FAILED), 3 deferred (duplicate cross-shelf enrichment fetches, unbounded getTrackedShows() fan-out, narrow token-refresh race in pointer RPC call), 13 dismissed; tsc clean → done
# story 3.1 implemented 2026-07-07: tracked_shows table (0006) + recompute_next_episode_pointer security-definer RPC (0007, first in codebase) + catalog-title/catalog.ts tmdbEpisodeId plumbing + trackedShows.ts + "I'm watching this" on TitleDetailScreen + Up Next shelf above Watchlist on HomeScreen with whole-page empty-state reconciliation; tsc/expo export/smoke-check pass; RPC algorithm verified directly against local stack (movie/tv branch, graceful degradation, chronological ordering, derive-from-full-watch-set, AC4 idempotency, security-definer guard, grant restrictions); manual on-device UI pass outstanding (no emulator) → review
# story 3.1 started 2026-07-07: status → in-progress
# story 3.1 drafted 2026-07-07: tracked_shows table + security-definer recompute_next_episode_pointer RPC (catalog_cache dependency, security hardening) + catalog-title tmdbEpisodeId plumbing + "I'm watching this" on title detail + Up Next shelf on Home (reuses 2.4's shelf pattern) + deferred whole-page empty-state reconciliation; corrects epics' stale RPC name (advance_ → recompute_) against ARCHITECTURE-SPINE.md AD-10; status → ready-for-dev
# story 3.2 drafted 2026-07-08: reuses 3.1's existing recompute_next_episode_pointer RPC (0007, already patched with p_media_type) unchanged — organic pointer advance is wired as a side effect of watchSync's per-row sync success (avoids racing the still-in-flight outbox drain, and reuses existing retry triggers for the offline case, AC2); adds a rounded-sm primary-fill uppercase Watched pill to TitleCard, gated to tv items with a non-null pointer on the Up Next shelf only (no film control — Story 3.3 — and no title-detail control, per NFR1's "tap on the Up Next card" framing); status → ready-for-dev
generated: 2026-07-02
last_updated: 2026-07-13
# code review of story 4.1 completed 2026-07-13: 3-layer adversarial review (Blind Hunter, Edge Case Hunter, Acceptance Auditor) against the story's 5-file diff; 3 patches applied (dead-end "pull to retry" copy → working retry button matching TitleDetailScreen's error-state convention; loadingMore-stuck-forever bug fixed by eagerly resetting it in load() when a reload supersedes an in-flight loadMore; loadMore() gained a synchronous loadingMoreRef re-entrancy guard against FlatList's onEndReached double-fire, mirroring the codebase's savingRef convention), 2 deferred (composite index on watches(watched_at,id) — already the story's own Open Question #2; DiaryRow's episode-label linear scan vs TitleDetailScreen's Map — negligible at scale), 10 dismissed (RLS/smoke-check already covers app-level filter concern, cursor values are always machine-generated so not injection-reachable, StarRating no-op onChange matches pre-existing 3.7 precedent, mood-chips-as-text matches already-approved 3.7 WatchRow precedent, others low-impact/out-of-scope); tsc/expo export re-verified clean → done
# story 4.1 implemented 2026-07-13: new getDiaryPage() in watchEdit.ts (keyset cursor on watched_at+id, immune to the offset-drift bug an insert/delete mid-scroll would cause on a live table — verified directly via a throwaway HTTP script against local PostgREST, including the exact mid-scroll-insert scenario); new DiaryScreen.tsx (infinite-scroll list, enrichment mandatorily deduped per title via a persistent Map cache — verified a rewatched title's fetchTitleDetail fires once per page, not once per row); new ProfileStack.tsx wiring a "Diary" row onto Profile ("You"), AppShell.tsx/ProfileScreen.tsx updated accordingly, bottom nav untouched. Reused EditWatchSheet/editWatch/removeWatch from 3.7 verbatim for AC5. tsc/expo export/smoke-check (22 checks, no new block) all clean. On-device UI pass outstanding (no emulator, consistent with every prior story). status → review
# story 4.1 started 2026-07-13: status → in-progress
# story 4.1 drafted 2026-07-13: extends app/data/watchEdit.ts with a keyset-paginated getDiaryPage() (cross-title generalization of 3.7's per-title getWatchesForTitle, using a watched_at+id cursor rather than offset pagination since the table mutates under the user while scrolling); new DiaryScreen.tsx (infinite-scroll list mirroring RecommendationsScreen's pattern, with enrichment mandatorily deduped per title since Diary rows are NOT distinct titles like every other shelf — a rewatch/bulk-log produces multiple rows per tmdb_id); reuses EditWatchSheet/editWatch/removeWatch from 3.7 verbatim for AC5. Resolves 3.7/3.8's flagged follow-up (Diary's bottom-nav slot was permanently repurposed for Recommendations in 3.8) by adding a "Diary" entry point on the Profile ("You") tab via a new ProfileStack.tsx, instead of reclaiming a tab slot — user-confirmed at drafting time via AskUserQuestion. No migration, no new dependency, no new smoke-check block expected. status → ready-for-dev
# story 3.6 implemented 2026-07-11: WatchReaction/assertValidReaction/setWatchReaction widened to carry note end-to-end (local UPDATE + PATCH branch); debounced/flush-on-dismiss/keyboard-aware note TextInput added to RatingPrompt.tsx (500-char cap + live counter, first KeyboardAvoidingView usage in the codebase); smoke-check.mjs's existing reaction-invariant block extended with two note-specific server-side assertions (round-trip, and no DB-level length CHECK). watchSync.ts confirmed already-correct and left untouched. No migration. tsc/expo export/smoke-check all clean; on-device manual pass (debounce feel, keyboard avoidance, 500-char input cutoff) outstanding — no emulator in this environment, consistent with every prior Epic 3 story. status → review
# story 3.6 started 2026-07-11: status → in-progress
# story 3.3 drafted 2026-07-08: closes the exact gap 3.2's own scope wall named as 3.3's job — a tracked film sitting in Up Next has no Watched action (its pointer is permanently null, so 3.2's tv-only pointer-gated pill never renders for it). Widens HomeScreen's Watched-pill gate to unconditionally cover movie items and fixes handleMarkWatched's hardcoded mediaType:'tv'/tmdbEpisodeId — no other file touched (logWatch, watchSync's RPC gate, and TitleCard's pill are already fully media-type-generic). Explicitly declines a title-detail Watched control (not named by any AC, matches 3.2's own precedent of punting that table row). status → ready-for-dev
# story 3.3 started 2026-07-08: status → in-progress
# story 3.3 implemented 2026-07-08: handleMarkWatched now forwards item's real mediaType + film-null tmdbEpisodeId; Up Next shelf's Watched pill gate widened to cover tracked films unconditionally alongside tv's existing pointer gate; single file touched (HomeScreen.tsx); tsc/expo export/smoke-check clean; film watches-row shape + rewatch/AD-3 independence verified directly against local Supabase stack; watchSync.ts's tv-only recompute gate confirmed unchanged by source inspection; on-device manual pass (pill rendering, soft confirmation) outstanding (no emulator) → review
# story 3.2 implemented 2026-07-08: real tmdb_episode_id through the outbox (watchLog.ts) + organic pointer-advance as a watchSync.ts post-upsert side effect (RECOMPUTE_TIMEOUT_MS, per-pass dedupe, own try/catch) + ✓ Watched pill on TitleCard (watched-badge, rounded-sm/primary-fill/uppercase) + wired into Up Next shelf only (HomeScreen UpNextItem, gated tv+non-null-pointer) + Home's soft confirmation (mirrors TitleDetailScreen); tsc/expo export/smoke-check clean; RPC chain (advance → caught-up-null → film-no-op) + AC3 fields + AC4 no-raw-PATCH grep verified directly against local Supabase stack (migrations re-applied to pick up 3.1's p_media_type patch); on-device manual pass outstanding (no emulator) → review
# story 3.2 started 2026-07-08: status → in-progress
# code review of story 2.4 completed 2026-07-06: 3-layer adversarial review (Blind Hunter, Edge Case Hunter, Acceptance Auditor); all 4 ACs conformant, no scope-wall violations; 3 patches applied to HomeScreen.load (all-enrichment-fail false-empty → error state, requestSeq guard vs stale focus-refetch, background-refresh instead of spinner flicker), 1 deferred (unbounded fan-out — spec punts to future optimization), 4 dismissed; tsc + android export clean → done
# story 2.4 implemented 2026-07-06: getWatchlist() (throws on failure) + HomeStack (mirrors AddStack) + HomeScreen rewrite (Watchlist shelf, useFocusEffect refetch); tsc/expo export clean; smoke-check + on-device outstanding (no local stack/emulator) → review
# story 2.4 drafted 2026-07-06: reuses fetchTitleDetail for shelf enrichment (no new endpoint), adds HomeStack (mirrors AddStack) so Home can push TitleDetail, retires the 1.3 static empty-state in favor of the shelf's own empty copy; status → ready-for-dev
# code review of story 2.3 completed 2026-07-06: 3-layer adversarial review (all 4 ACs conformant); 6 patches applied for optimistic-toggle races (in-flight-lookup clobber, add/remove serialization, same-frame double-tap, mountedRef guard), 1 deferred (pre-existing 2.2 Poster state), 3 dismissed; tsc clean → done
# story 2.3 reconciled 2026-07-06: implementation (already committed in 6437091) verified task-by-task against spec; tsc/expo export gates pass; smoke-check + on-device pass outstanding (no local stack/emulator in this env) → review
# story 2.3 started 2026-07-06: status → in-progress
# story 2.1 marked done 2026-07-05
# story 2.2 started 2026-07-05: status → in-progress
# story 2.2 implemented 2026-07-05: catalog-title fn + detail screen + AddStack (native-stack dep) + shared TitleCard; tsc/export clean; on-device manual pass outstanding → review
# code review of story 1.5 completed 2026-07-05: 7 patches applied, status → done
# story 2.1 verified 2026-07-05: all 4 ACs already satisfied by 1.4/1.5/1.6 code, no changes needed, status → review
# story 2.2 drafted 2026-07-05: ready-for-dev
# story 2.3 drafted 2026-07-06: watchlist_items table + direct-PostgREST watchlist.ts + ❤️ on TitleCard/detail; status → ready-for-dev
project: popcorn-time
project_key: NOKEY
tracking_system: file-system
story_location: "{project-root}/_bmad-output/implementation-artifacts"

development_status:
  epic-1: in-progress
  1-1-project-foundation-boots-locally: done
  1-2-create-an-account-and-sign-in-google-free: done
  1-3-themed-app-shell-with-bottom-navigation-dark-mode: done
  1-4-search-a-real-title-through-the-proxied-catalog: done
  1-5-log-a-watch-local-first-surviving-a-network-drop: done
  1-6-prove-the-private-by-default-wall: done
  epic-1-retrospective: optional

  epic-2: in-progress
  2-1-full-search-experience: done
  2-2-title-detail-for-films-and-shows: done
  2-3-save-a-title-to-the-watchlist: done
  2-4-watchlist-shelf-on-home: done
  epic-2-retrospective: optional

  epic-3: in-progress
  3-1-track-a-show-or-film-into-up-next: done
  3-2-one-tap-watched-advances-the-pointer: done
  3-3-log-a-film-in-one-action: done
  3-4-bulk-log-a-whole-season: done
  3-5-rate-and-react-after-a-watch: done
  3-6-attach-an-optional-note-to-a-watch: done
  3-7-edit-or-remove-a-logged-watch: done
  3-8-optional-recommendations-shelf-non-blocking: done
  epic-3-retrospective: optional

  epic-4: in-progress
  4-1-the-diary-your-chronological-watch-history: done
  4-2-profile-you-stats-favorites-watchlist-friends: backlog
  4-3-settings-switch-to-paper-white-persisted: backlog
  4-4-edit-your-profile-display-name-and-avatar: backlog
  epic-4-retrospective: optional

  epic-5: backlog
  5-1-follow-friends-and-find-them: backlog
  5-2-opt-in-to-sharing-my-activity: backlog
  5-3-the-chronological-activity-feed: backlog
  5-4-add-to-watchlist-from-a-feed-entry: backlog
  5-5-shared-lists: backlog
  epic-5-retrospective: optional

  epic-6: backlog
  6-1-per-title-notify-bell-and-global-toggle: backlog
  6-2-caught-up-nudge: backlog
  6-3-device-registration-and-per-build-push-channel: backlog
  6-4-daily-new-episode-poller-and-fan-out: backlog
  epic-6-retrospective: optional

  epic-7: backlog
  7-1-export-my-data: backlog
  7-2-delete-my-account: backlog
  epic-7-retrospective: optional
