{
  "name": "popcorn-time",
  "version": "0.1.0",
  "private": true,
  "description": "Popcorn Time — private-by-default film & TV tracker.",
  "packageManager": "pnpm@10.28.2",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "supabase:up": "docker compose -f supabase/docker-compose.yml --project-directory supabase up -d",
    "supabase:down": "docker compose -f supabase/docker-compose.yml --project-directory supabase down",
    "supabase:keys": "node supabase/scripts/generate-keys.mjs",
    "supabase:migrate": "node supabase/scripts/apply-migrations.mjs",
    "app": "pnpm --filter popcorn-time start",
    "verify": "node scripts/smoke-check.mjs",
    "build:android": "pnpm --dir app exec eas build --platform android --profile production",
    "build:ios": "pnpm --dir app exec eas build --platform ios --profile production"
  }
}
