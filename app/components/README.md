# app/components

Shared UI primitives (buttons, cards, list rows, the themed shell pieces) used
across feature modules.

Empty for the Story 1.1 scaffold — the first primitives arrive with the themed
app shell and bottom navigation in Story 1.3.
