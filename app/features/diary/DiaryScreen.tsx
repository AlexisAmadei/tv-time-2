// Diary — themed placeholder (Story 1.3). The full chronological watch history
// + its warm empty state belong to Epic 4 (Story 4.1).

import { PlaceholderScreen } from '../../components/PlaceholderScreen';

export default function DiaryScreen() {
  return <PlaceholderScreen title="Diary" subtitle="Your watch history will live here." />;
}
