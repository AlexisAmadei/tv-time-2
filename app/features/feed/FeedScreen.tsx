// Feed — themed placeholder (Story 1.3). The chronological activity feed + its
// warm empty state belong to Epic 5 (Story 5.3).

import { PlaceholderScreen } from '../../components/PlaceholderScreen';

export default function FeedScreen() {
  return <PlaceholderScreen title="Feed" subtitle="Friends’ activity will appear here." />;
}
